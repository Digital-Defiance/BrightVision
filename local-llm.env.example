# Copy to local-llm.env in this directory (gitignored). Bright Vision reads on launch.
# You do not need the separate local-llm git repo or local-llm.sh — use Terminal → Local LLM in the app.

OLLAMA_HOST=http://127.0.0.1:11434
DATA_MODEL=qwen3.6:27b-q4_K_M

# Settings → LLM model should be ollama_chat/<DATA_MODEL>
# Optional: INDEX_MODEL / EMBEDDING_MODEL are ignored unless you use the local-llm stack elsewhere.
